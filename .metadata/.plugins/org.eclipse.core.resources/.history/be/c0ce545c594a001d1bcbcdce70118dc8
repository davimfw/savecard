package app;

import static spark.Spark.*;
import dao.UsuarioDAO;

public class HelloWorld {

	public static void main(String[] args) {
		port(4568);
		UsuarioDAO dao = new UsuarioDAO();
		dao.getAll();
        get("/hello", (req, res) -> "Opa World " + dao.getAll());
    }
}
